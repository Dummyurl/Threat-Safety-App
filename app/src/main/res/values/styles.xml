<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="RatingBarSecurity" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorControlNormal">@color/color_gray_light</item>
        <item name="colorControlActivated">@color/green</item>

    </style>

    <style name="RatingBarThreat" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorControlNormal">@color/color_gray_light</item>
        <item name="colorControlActivated">@color/red</item>

    </style>

    <style name="RatingBarPower" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorControlNormal">@color/color_gray_light</item>
        <item name="colorControlActivated">@color/yellow</item>

    </style>



</resources>
